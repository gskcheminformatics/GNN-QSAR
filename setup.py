from setuptools import setup, find_packages

setup(name='make_data',
   version='1.0.0',
   description='Graph QSAR Analysis custom datasets',
   url='',
   author = 'Natasha Sanjrani',
   packages=find_packages()
)
